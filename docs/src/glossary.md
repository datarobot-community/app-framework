# Glossary

Key terms and concepts used throughout the App Framework documentation.

---

## A

**Answer file**
: The YAML file written to `.datarobot/answers/COMPONENT-NAME.yml` when you apply a component. It records the choices you made during the copier wizard — template name, code name, port, LLM type, and so on. Passing this file to `dr component update` or `uvx copier update -a` replays your configuration non-interactively, enabling automated updates without re-answering the wizard.

**App Framework**
: The DataRobot Application Framework for accelerating and making production grade agentic AI applications.

**`af-component-*` (naming convention)**
: <!-- TODO: Naming convention for App Framework Components [link to def] -->


---

## C


**Component**
: A [copier](#copier)-style template — a top-level folder or file set — that you layer into an application and is built on af-component-base to add one capability. Each component lives in its own repository named `af-component-*`, runs an interactive wizard when applied, defines its own dependencies in copier-module.yaml and records the answers to an [answer file](#answer-file) in `.datarobot/answers/`. Components are the primary building block of every App Framework application.

**`copier-module.yaml`**
: An App Framework specific configuration file that defines the upstream and possible peer dependencies of an af-component.

**`copier-watch`**
: <!-- TODO: this is a utility for copier that makes testing out component changes easier by automatically applying them to an App Framework app -->

**Copier**
: The open-source templating tool ([copier.readthedocs.io](https://copier.readthedocs.io)) that the App Framework uses to apply and update component templates. Unlike Yeoman or Cookiecutter, Copier has Git-native update semantics: it records the source template's commit hash in the answer file and can merge upstream template changes into your recipe with `copier update`, reducing merge conflicts on re-application.

**Core library package**
: An optional shared `core/` Python package generated by `af-component-base` when you answer *yes* to the "Include core library" wizard question. The package lives at the recipe root and provides common utilities — auth helpers, config loading, shared types — that other components in the same multi-component recipe can import rather than duplicating code. Enable it when your recipe will include more than one backend component that needs shared logic.

**Custom Application**
: <!-- TODO: Searchs docs.datarobot.com for the right link to this  -->

---

## D

**DataRobot CLI (`dr`)**
: <!-- TODO: it is the official DataRobot Command Line: cli.datarobot.com for more information -->

**`datarobot-pulumi-utils`**
: <!-- TODO: A DataRobot library that extends our Pulumi provider (link to the official Pulumi DataRobot docs and also link to the github repo for datarobot-pulumi-utils) -->

**`.datarobot/` directory**
: <!-- TODO: The hidden folder for all datarobot cli and datarobot app framework configuration and data -->

**Declarative API**
: <!-- TODO: Search docs.datarobot.com for the link to this. It is our IaC "product". i.e., Terraform and Pulumi clients for the DataRobot platform -->

---

## F

**Foundation application template**
: <!-- TODO: One of our official open source applications. Link to the github.com/datarobot-community's README that has the section header for this -->

---

## I

**`infra/` directory**
: <!-- TODO: Part of the base component that stores the Pulumi IaC to define DataRobot platform resources such as VDBs, Models, Agents, and Applications -->

---

## L

**LLM Deployment**
: <!-- TODO: search and add relevant link to docs.datarobot.com for LLM deployments and LLM Blueprints -->

**LLM Gateway**
: <!-- TODO: searchs and add relevant link to docs.datarobot.com for the LLM Gateway -->

**Low-code YAML agents vs. code-based agents**
: Two modes offered by `af-component-agent`. The **NeMo Agent Toolkit (NAT)** variant (`nat`) lets you define agent behavior, tools, and conversation flows in declarative YAML — no Python required <!-- Add link to NVIDIA's NAT Website -->. This is the "low-code" path and is well-suited for straightforward Q&A or task-routing agents. The code-based variants — **CrewAI** (`crewai`), **LangGraph** (`langgraph`), and **LlamaIndex** (`llamaindex`) <!--Link to the web pages of these three Agentic frameworks--> — give you full Python control over agent logic, state, memory, and tool use, at the cost of more boilerplate. Choose NAT when you want to move fast and avoid framework complexity; choose a code-based framework when you need fine-grained orchestration or custom tooling.

---

## N

**NeMo Agent Toolkit (NAT)**
: <!-- TODO: grab the definition from NVIDIAs NAT Web page -->

---

## P

**`pulumi-datarobot`**
: <!-- TODO: link to the github repo for datarobot's pulumi-datarobot and the official documentation for it on Pulumi's website -->

---

## R

**Recipe**
: <!-- TODO: A composition of af-components that have the intended purpose of being forked and their own templates for builders to build from -->

**Repeatable component**
: <!-- TODO: An AF Component [link] that can be used multiple times for application. For example if you want to deploy multiple agents as part of the same agentic ai application -->

---

## T

**`Taskfile.yaml`**
: <!-- TODO: Link to go-task's Website and describe that it defines tasks similar to mise or Makefile -->

---

## U

**Use case**
: <!-- TODO: Link to the docs.datarobot.com website documentation for what a Use Case is -->
